This project is developed during my study on Ionic.

## How to  Run this Project:

```bash
$ npm install -g ionic cordova
```

Then, cd into `ionic-doc-study` and run:

```bash
$ npm install
$ ionic serve
```


